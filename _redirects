# Canonical redirect: /index.html → / (avoid duplicate content)
/index.html   /   301

# Clean URLs — redirect if someone lands on .html version of key pages
# (optional — remove if you prefer .html URLs in your links)
# /enroll       /enroll.html      200
# /faq          /faq.html         200

# 404 fallback
/404    /index.html    404
